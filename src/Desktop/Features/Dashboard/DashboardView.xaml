<UserControl x:Class="Desktop.Features.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Desktop.Features.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <StackPanel>
        <!-- Top Metrics — 5 KPI Cards -->
        <Grid Margin="0,0,0,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Revenue -->
            <Border Grid.Column="0" Style="{StaticResource CardContainer}" Margin="0,0,12,0" Padding="20">
                <StackPanel>
                    <Grid>
                        <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                            <materialDesign:PackIcon Kind="CurrencyUsd" Foreground="{DynamicResource PrimaryBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <TextBlock Text="Receita Total" FontSize="12" Foreground="{StaticResource MutedTextBrush}" FontWeight="Medium" Margin="0,14,0,4"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat='R$ {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <materialDesign:PackIcon Kind="TrendingUp" Foreground="{StaticResource SuccessBrush}" Width="14" Height="14" VerticalAlignment="Center"/>
                        <TextBlock Text="12% vs mês ant." FontSize="10.5" Foreground="{StaticResource SuccessBrush}" Margin="4,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Profit -->
            <Border Grid.Column="1" Style="{StaticResource CardContainer}" Margin="0,0,12,0" Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource SuccessLightBrush}" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="TrendingUp" Foreground="{StaticResource SuccessBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Lucro Estimado" FontSize="12" Foreground="{StaticResource MutedTextBrush}" FontWeight="Medium" Margin="0,14,0,4"/>
                    <TextBlock Text="{Binding TotalProfit, StringFormat='R$ {0:N2}'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                    <TextBlock Text="Margem média: 35%" FontSize="10.5" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" FontWeight="Medium"/>
                </StackPanel>
            </Border>

            <!-- Orders -->
            <Border Grid.Column="2" Style="{StaticResource CardContainer}" Margin="0,0,12,0" Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="ShoppingOutline" Foreground="{StaticResource PrimaryBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Total Pedidos" FontSize="12" Foreground="{StaticResource MutedTextBrush}" FontWeight="Medium" Margin="0,14,0,4"/>
                    <TextBlock Text="{Binding OrderCount}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="Período ativo" FontSize="10.5" Foreground="{StaticResource MutedTextBrush}" Margin="0,8,0,0" FontWeight="Medium"/>
                </StackPanel>
            </Border>

            <!-- Shipping -->
            <Border Grid.Column="3" Style="{StaticResource CardContainer}" Margin="0,0,12,0" Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource InfoLightBrush}" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="TruckDeliveryOutline" Foreground="{DynamicResource InfoBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Envios SuperFrete" FontSize="12" Foreground="{StaticResource MutedTextBrush}" FontWeight="Medium" Margin="0,14,0,4"/>
                    <TextBlock Text="{Binding TotalLabelsCount}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Border Style="{StaticResource BadgeInfo}" Padding="6,2" Margin="0,0,4,0">
                            <TextBlock Text="{Binding LabelsToPostCount, StringFormat='{} {0} pendentes'}" FontSize="10" Foreground="{DynamicResource InfoBrush}" FontWeight="SemiBold"/>
                        </Border>
                        <Border Style="{StaticResource BadgeSuccess}" Padding="6,2">
                            <TextBlock Text="{Binding LabelsInTransitCount, StringFormat='{} {0} em rota'}" FontSize="10" Foreground="{DynamicResource SuccessBrush}" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Low Stock -->
            <Border Grid.Column="4" Style="{StaticResource CardContainer}" Padding="20">
                <StackPanel>
                    <Border Background="{DynamicResource DangerLightBrush}" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="{StaticResource DangerBrush}" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Estoque Baixo" FontSize="12" Foreground="{StaticResource MutedTextBrush}" FontWeight="Medium" Margin="0,14,0,4"/>
                    <TextBlock Text="{Binding LowStockProductsCount}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource DangerBrush}"/>
                    <TextBlock Text="Ação necessária" FontSize="10.5" Foreground="{StaticResource DangerBrush}" Margin="0,8,0,0" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bottom Section -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Recent Orders -->
            <Border Grid.Column="0" Style="{StaticResource CardContainer}" Margin="0,0,20,0" Padding="28">
                <StackPanel>
                    <Grid Margin="0,0,0,24">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ClockOutline" Foreground="{StaticResource PrimaryBrush}" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Pedidos Recentes" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                        </StackPanel>
                        <Button Style="{StaticResource SecondaryButton}" Content="VER TUDO" HorizontalAlignment="Right" FontSize="11" Height="30" Padding="12,0"/>
                    </Grid>
                    
                    <ListView ItemsSource="{Binding RecentOrders}" BorderThickness="0" Background="Transparent">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0,10"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtleBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" CornerRadius="6">
                                                <ContentPresenter />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="PEDIDO" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding OrderId}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" FontSize="13"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="CLIENTE" Width="220">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding CustomerName}" FontWeight="Medium" Foreground="{StaticResource TextBrush}" FontSize="13"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="TOTAL" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Total, StringFormat='R$ {0:N2}'}" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" FontSize="13"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="STATUS" Width="140">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="6" Padding="8,3" Background="{DynamicResource BorderSubtleBrush}" HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding Status}" FontSize="10.5" FontWeight="SemiBold" Foreground="{DynamicResource StrongMutedTextBrush}"/>
                                            </Border>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </Border>

            <!-- Sidebar: Sales by Source + CTA -->
            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource CardContainer}" Padding="28" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Vendas por Canal" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,24"/>
                        <ItemsControl ItemsSource="{Binding SalesBySource}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,20">
                                        <Grid>
                                            <TextBlock Text="{Binding Source}" FontWeight="Medium" Foreground="{StaticResource TextBrush}" FontSize="12.5"/>
                                            <TextBlock Text="{Binding Revenue, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" FontWeight="SemiBold" Foreground="{StaticResource StrongMutedTextBrush}" FontSize="12.5"/>
                                        </Grid>
                                        <Grid Margin="0,8,0,0">
                                            <Border Background="{DynamicResource BorderSubtleBrush}" Height="6" CornerRadius="3"/>
                                            <Border Background="{DynamicResource PrimaryBrush}" Height="6" CornerRadius="3" HorizontalAlignment="Left" Width="{Binding Percentage, ConverterParameter=200, Converter={StaticResource WidthPercentageConverter}}"/>
                                        </Grid>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- CTA Card -->
                <Border Padding="28" CornerRadius="14">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366F1" Offset="0"/>
                            <GradientStop Color="#8B5CF6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.18" Color="#6366F1"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="Pronto para crescer?" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Crie um novo pedido agora para impulsionar suas vendas e gerir seu estoque." Margin="0,10,0,24" TextWrapping="Wrap" Foreground="White" Opacity="0.85" LineHeight="20" FontSize="13"/>
                        <Button Style="{StaticResource ModernButton}" Content="NOVO PEDIDO" Background="White" Foreground="#6366F1" FontWeight="Bold" Height="42"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>