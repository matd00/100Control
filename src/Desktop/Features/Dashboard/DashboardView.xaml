<UserControl x:Class="Desktop.Features.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Desktop.Features.Dashboard"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <StackPanel>
        <Grid Margin="0,0,0,32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0,0,12,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CurrencyUsd" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="24" Height="24"/>
                    <TextBlock Text="Total Revenue" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" Margin="0,12,0,4"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat='R$ {0:N2}'}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <materialDesign:PackIcon Kind="ArrowUp" Foreground="{StaticResource SuccessBrush}" Width="16" Height="16" VerticalAlignment="Center"/>
                        <TextBlock Text="12% from last month" FontSize="11" Foreground="{StaticResource SuccessBrush}" Margin="4,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Margin="12,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <materialDesign:PackIcon Kind="TrendingUp" Foreground="{StaticResource SuccessBrush}" Width="24" Height="24"/>
                    <TextBlock Text="Estimated Profit" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" Margin="0,12,0,4"/>
                    <TextBlock Text="{Binding TotalProfit, StringFormat='R$ {0:N2}'}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                    <TextBlock Text="Average margin: 35%" FontSize="11" Opacity="0.5" Margin="0,8,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" Margin="12,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <materialDesign:PackIcon Kind="ShoppingOutline" Foreground="{StaticResource PrimaryBrush}" Width="24" Height="24"/>
                    <TextBlock Text="Total Orders" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" Margin="0,12,0,4"/>
                    <TextBlock Text="{Binding OrderCount}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold"/>
                    <TextBlock Text="Active period" FontSize="11" Opacity="0.5" Margin="0,8,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="3" Margin="12,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Label" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="24" Height="24"/>
                    <TextBlock Text="SuperFrete" FontSize="12" Foreground="#64748B" Margin="0,12,0,4"/>
                    <TextBlock Text="{Binding TotalLabelsCount}" FontSize="24" FontWeight="Bold" Foreground="#1E293B"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Border Background="#DBEAFE" CornerRadius="4" Padding="4,2" Margin="0,0,4,0">
                            <TextBlock Text="{Binding LabelsToPostCount, StringFormat='{} {0} a postar'}" FontSize="9" Foreground="#1E40AF" FontWeight="Bold"/>
                        </Border>
                        <Border Background="#DCFCE7" CornerRadius="4" Padding="4,2">
                            <TextBlock Text="{Binding LabelsInTransitCount, StringFormat='{} {0} em trânsito'}" FontSize="9" Foreground="#166534" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="4" Margin="12,0,0,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="{StaticResource DangerBrush}" Width="24" Height="24"/>
                    <TextBlock Text="Low Stock" Style="{StaticResource MaterialDesignBody2TextBlock}" Opacity="0.6" Margin="0,12,0,4"/>
                    <TextBlock Text="{Binding LowStockProductsCount}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" FontWeight="Bold" Foreground="{StaticResource DangerBrush}"/>
                    <TextBlock Text="Attention needed" FontSize="11" Opacity="0.5" Margin="0,8,0,0"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Recent Activity -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,24,0" ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16">
                <StackPanel>
                    <Grid Margin="0,0,0,24">
                        <TextBlock Text="Recent Orders" Style="{StaticResource MaterialDesignHeadline6TextBlock}" FontWeight="Bold"/>
                        <Button Style="{StaticResource MaterialDesignFlatButton}" Content="VIEW ALL" HorizontalAlignment="Right" FontSize="12"/>
                    </Grid>
                    
                    <ListView ItemsSource="{Binding RecentOrders}" BorderThickness="0" Background="Transparent">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Order ID" DisplayMemberBinding="{Binding OrderId}" Width="100"/>
                                <GridViewColumn Header="Customer" DisplayMemberBinding="{Binding CustomerName}" Width="200"/>
                                <GridViewColumn Header="Total">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Total, StringFormat='R$ {0:N2}'}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Status" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="12" Padding="12,4" Background="#F1F5F9">
                                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold" Foreground="#475569" HorizontalAlignment="Center"/>
                                            </Border>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </materialDesign:Card>

            <!-- Sales by Source -->
            <StackPanel Grid.Column="1">
                <materialDesign:Card ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16" Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="Sales by Channel" Style="{StaticResource MaterialDesignHeadline6TextBlock}" FontWeight="Bold" Margin="0,0,0,24"/>
                        <ItemsControl ItemsSource="{Binding SalesBySource}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,20">
                                        <Grid>
                                            <TextBlock Text="{Binding Source}" FontWeight="Medium"/>
                                            <TextBlock Text="{Binding Revenue, StringFormat='R$ {0:N2}'}" HorizontalAlignment="Right" Opacity="0.7"/>
                                        </Grid>
                                        <ProgressBar Value="{Binding Percentage}" Height="6" Margin="0,8,0,0" 
                                                     materialDesign:TransitionAssist.DisableTransitions="False"
                                                     Foreground="{DynamicResource PrimaryHueMidBrush}" Background="#F1F5F9" BorderThickness="0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card ContentControl.Padding="24" materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="16" Background="{DynamicResource PrimaryHueMidBrush}" Foreground="White">
                    <StackPanel>
                        <TextBlock Text="Ready for more?" Style="{StaticResource MaterialDesignHeadline6TextBlock}" FontWeight="Bold"/>
                        <TextBlock Text="Create a new order now to boost your sales." Margin="0,8,0,24" TextWrapping="Wrap" Opacity="0.8"/>
                        <Button Style="{StaticResource MaterialDesignFlatMidPrimaryButton}" Content="NEW ORDER" Background="White" Foreground="{DynamicResource PrimaryHueMidBrush}" FontWeight="Bold"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>