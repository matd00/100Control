<UserControl x:Class="Desktop.Features.Customers.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Desktop.Infrastructure.Converters"
             Background="Transparent">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:BoolToEditTextConverter x:Key="BoolToEditText"/>
        <Style x:Key="CBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CTb" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="360"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Style="{StaticResource CardContainer}" Margin="0,0,16,0" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Margin="24,20,24,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Clientes" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                        <Border Style="{StaticResource BadgePrimary}" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Customers.Count}" FontSize="11" Foreground="{DynamicResource PrimaryBrush}" FontWeight="SemiBold"/>
                        </Border>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedTextBrush}" Margin="16,0,0,0" VerticalAlignment="Center" FontSize="12"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CTb}" Width="240" ToolTip="Buscar por Nome, Email, Telefone ou Documento"/>
                        <Button Content="✕" Command="{Binding ClearSearchCommand}" Background="{DynamicResource BorderSubtleBrush}" Foreground="{DynamicResource StrongMutedTextBrush}" Style="{StaticResource CBtn}" Padding="8,4" Margin="6,0,0,0" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
                <Border Grid.Row="1" Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,1" Padding="24,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="NOME / EMAIL" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBlock Grid.Column="1" Text="TELEFONE" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}"/>
                        <TextBlock Grid.Column="2" Text="CIDADE" FontSize="10.5" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}"/>
                    </Grid>
                </Border>
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Customers}" Padding="8,2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,0,0,1" Padding="16,11" Cursor="Hand" Background="Transparent" x:Name="Row" CornerRadius="6">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.SelectCustomerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
                                            <TextBlock Text="{Binding Email}" FontSize="12" Foreground="{DynamicResource PlaceholderBrush}"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding Phone}" Foreground="{StaticResource StrongMutedTextBrush}" VerticalAlignment="Center" FontSize="13"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Location}" Foreground="{StaticResource StrongMutedTextBrush}" VerticalAlignment="Center" FontSize="13"/>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Row" Property="Background" Value="{DynamicResource BackgroundBrush}"/>
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Border Style="{StaticResource CardContainer}">
                <StackPanel>
                    <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToEditText}}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,18"/>
                    <TextBlock Text="Nome Completo *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <TextBlock Text="Email *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <TextBlock Text="Telefone *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <TextBlock Text="CPF/CNPJ *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding Document, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>
                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="0,14,0,0" Margin="0,4,0,0">
                        <TextBlock Text="ENDEREÇO PARA ENTREGA" FontSize="10.5" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                    </Border>
                    <TextBlock Text="CEP *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,14,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding ZipCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" MaxLength="9"/>
                    <TextBlock Text="Rua/Logradouro *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,6,0">
                            <TextBlock Text="Número *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource CTb}" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <TextBlock Text="Complemento" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource CTb}" Text="{Binding Complement, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="Bairro *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                    <TextBox Style="{StaticResource CTb}" Text="{Binding District, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,6,0">
                            <TextBlock Text="Cidade *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource CTb}" Text="{Binding City, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="6,0,0,0">
                            <TextBlock Text="UF *" FontSize="12" FontWeight="Medium" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource CTb}" Text="{Binding State, UpdateSourceTrigger=PropertyChanged}" MaxLength="2"/>
                        </StackPanel>
                    </Grid>
                    <Border Background="{DynamicResource WarningLightBrush}" CornerRadius="8" Padding="12" Margin="0,6,0,16" BorderBrush="{DynamicResource WarningBrush}" BorderThickness="1">
                        <TextBlock Text="Campos com * são obrigatórios para gerar etiqueta de frete." FontSize="11" Foreground="{DynamicResource WarningBrush}" TextWrapping="Wrap"/>
                    </Border>
                    <Button Content="Salvar Cliente" Command="{Binding CreateCustomerCommand}" Background="{DynamicResource PrimaryBrush}" Style="{StaticResource CBtn}" HorizontalAlignment="Stretch" Visibility="{Binding IsEditing, Converter={StaticResource InverseBoolToVisibility}}"/>
                    <StackPanel Orientation="Horizontal" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
                        <Button Content="Atualizar" Command="{Binding UpdateCustomerCommand}" Background="{DynamicResource PrimaryBrush}" Style="{StaticResource CBtn}" Margin="0,0,8,0"/>
                        <Button Content="Excluir" Command="{Binding DeleteCustomerCommand}" Background="{DynamicResource DangerBrush}" Style="{StaticResource CBtn}" Margin="0,0,8,0"/>
                        <Button Content="Cancelar" Command="{Binding ClearFormCommand}" Background="{DynamicResource BorderSubtleBrush}" Foreground="{DynamicResource StrongMutedTextBrush}" Style="{StaticResource CBtn}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
